# --- environments ---
.venv/
ucc-venv/
__pycache__/
*.pyc
.pytest_cache/

# --- data: the repo ships code + a small sample + a fetch script, never gigabytes ---
*.duckdb
*.duckdb.wal
raw_pages/
checkpoints/
*.parquet

# --- the published SQLite export is deliberately NOT ignored ---
!docs/ucc.db

# --- private planning / career documents: never publish ---
UCC_DEMO_RUNBOOK.md
artifactdemo.md
artifact-data-verification.md
*resume*.md
*linkedin*.md
*contract work*.md

# P6: the answer key stays private until the labels are in. Its sha256 is committed at
# docs/labels_key.sha256 BEFORE labelling, which proves the strata were fixed in advance.
labels_key.csv
