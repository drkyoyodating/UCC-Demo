# DECISIONS — append-only

One line per decision. Every pre-registration is appended BEFORE the thing it
governs runs; the commit hash is the timestamp. This file is the only artifact
that survives a context compaction mid-project.

## P0 — 2026-08-30
- Repo target: `drkyoyodating/UCC-Demo`, public, `main`. The YoYoMVP1.0 remote is never added (its history holds an unrotated Firebase admin key; GitHub visibility is per-repo, not per-branch). Verified: exactly one remote.
- Interpreter: `/opt/homebrew/bin/python3.14` (3.14.7). venv at `.venv/`. Install is `--only-binary=:all:` against a pinned requirements.txt.
- Pins: splink 4.0.16, duckdb 1.4.5, pandas 2.3.3. VERIFIED INSTALLED. Rationale: splink declares `pandas>=1.3.5` with no upper bound, so an unpinned resolve takes pandas 3.0.x — a breaking major — and fails mid-P5 looking exactly like the documented zero-match trap.
- Deviation from runbook §1.6: the cut-list packages (polars, sklearn, xgboost, geopandas, shapely, fastapi, sqlalchemy, psycopg2, dagster, dbt-core, scrapy, playwright, langgraph, openai) are NOT installed. §1.6 itself concedes they are unused. `dagster` additionally declares Requires-Python <3.14 and could not have been installed here at all.
- Optional, non-blocking: datasette + vl-convert-python installed separately so their failure cannot block the load-bearing pipeline.
- Splink API contract pinned at `src/splink_contract.py`, generated by introspecting the INSTALLED package. The public docs site documents Splink 5; resolve disagreements against the contract file.
- SEED = 20260830, fixed project-wide. EM in 4.0.16 has no RNG; the only stochastic step is `estimate_u_using_random_sampling(seed=...)`. Seeded, the pipeline is deterministic — required for the P6 stability number to measure clustering churn rather than our own sampling noise.
- LICENSE: MIT, code only, with an explicit Colorado open-data attribution and non-affiliation notice.
- `.gitignore` excludes UCC_DEMO_RUNBOOK.md and the Desktop career docs (they contain interview-odds framing and the drafted outreach email); `docs/ucc.db` is deliberately NOT ignored.
